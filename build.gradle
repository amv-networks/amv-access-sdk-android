buildscript {
    repositories {
        jcenter()
        google()
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.2.1'
        classpath 'com.github.dcendents:android-maven-gradle-plugin:2.0' // needed for jitpack
        classpath 'com.netflix.nebula:nebula-release-plugin:6.1.1'
        classpath 'com.netflix.nebula:nebula-project-plugin:3.4.0'
        classpath 'com.netflix.nebula:gradle-info-plugin:3.6.0'
    }
}

apply plugin: 'nebula.release'
apply plugin: 'nebula.project'
apply plugin: 'nebula.info'

task clean(type: Delete) {
    delete rootProject.buildDir
}

allprojects {
    repositories {
        mavenLocal()
        jcenter()
        google()
        maven {
            url "http://jfrog.high-mobility.com/artifactory/gradle-release-local/"
            //url "http://jfrog.high-mobility.com/artifactory/gradle-00/"
        }
    }

    project.ext {
        buildToolsVersion = '28.0.3'

        minSdkVersion = 23
        targetSdkVersion = 27
        compileSdkVersion = 28

        versionCode = 2
        versionName = '1.1'

        amvAccessClientVersion = '0.12.0'
        //hmkitVersion = '1.0.13'
        // hmAutoApiVersion = '1.0.4'
        hmkitVersion = '1.6.4@aar'
        hmAutoApiVersion = '1.7.3'

        guavaVersion = '27.0.1-android'
        rxAndroidVersion = '2.0.2'
        rxJavaVersion = '2.1.12'
        concealVersion = '2.0.2@aar'
        lombokVersion = '1.16.20'

        leakcanaryVersion = '1.5.4'
        butterknifeVersion = '10.0.0'
        daggerVersion = '2.16'

        junitVersion = '4.12'
        mockitoVersion = '2.15.0'
        commonsLangVersion = '3.7'
        espressoVersion = '3.0.1'
        supportTestVersion = '1.0.1'
    }
}